import { BarLoader } from "react-spinners";

const Spinner = () => {
  return (
    <main className="h-dvh flex items-center justify-center">
      <BarLoader color="#0A7C6E" />
    </main>
  );
};

export default Spinner;
